/**
 * Person B — replace with SQLite/Postgres.
 * Skeleton keeps an in-memory note so structure is clear.
 */
export function getDbStatus() {
  return {
    driver: "none-yet",
    message: "TODO Person B: initialize SQLite/Postgres + seed",
  };
}
