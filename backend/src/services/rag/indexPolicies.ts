/**
 * Person C — build/load vector index from data/policies.
 * Call from a script or on API boot in development.
 */
export async function ensurePolicyIndex() {
  // TODO: chunk → embed → store
  return { ready: false, reason: "skeleton" };
}
